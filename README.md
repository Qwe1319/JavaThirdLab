
2. Запустить (по умолчанию 10000 операций):

```
java Main
```

3. Запустить с другими параметрами, например n операций, k повторения и l прогрева:

```
java Main n k l
```

Результат - таблица: метод, сколько раз выполнялся, среднее время (милисекунды) и стандартное отклонение (милисекунды).
